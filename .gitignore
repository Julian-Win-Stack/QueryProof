data/
node_modules/
gold/
.env
PLAN.md

# evalite's SQLite store — the UI and run-over-run diffing read from it.
# The durable evidence is runs/*.json, which IS committed.
.evalite/

# Next.js build output and its generated type shim
.next/
next-env.d.ts

# TypeScript incremental build artifact
tsconfig.tsbuildinfo

# macOS
.DS_Store
